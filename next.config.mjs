/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["lh3.googleusercontent.com","vkox05von4xvmzvf.public.blob.vercel-storage.com"],
      },
};

export default nextConfig;
